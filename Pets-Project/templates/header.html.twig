<div class="cover-container d-flex mx-auto flex-column" style="background-color: #FFF;">
  <div class="row navbar">
    <div class="col-sm">
      <nav class="navbar">
        <a href="{{path('home')}}">
          <img src="{{asset('img/logo.jpeg')}}" style="margin-left:25px" class="navbar-brand" width="90" height="60">
        </a>
      </nav>
    </div>
    {% if not is_granted("IS_AUTHENTICATED_FULLY")%}
    <div class="col-sm">
      <nav class="navbar ">
        <form class="form-inline">
          <input class="form-control mr-sm-2 rounded-pill" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-secondary" my-2 my-sm-0" type="submit">Search</button>
        </form>
      </nav>
    </div>
    {% endif %}
    <div class="col-md">
    <nav class="navbar navbar-expand-lg">
      <div class="collapse navbar-collapse">
        <div class="navbar-nav text-right">

          {% if not is_granted("IS_AUTHENTICATED_FULLY")%}
            <a class="nav-item nav-link text-muted" href="{{path('ayuda')}}">Ayuda</a> 
            <a class="nav-item nav-link text-muted" href="{{path('nosotros')}}">Nosotros</a>
            <a class="nav-item nav-link text-muted active" href="{{path('home')}}">Home</a>
            <a class="nav-item nav-link text-muted" href="{{path('fos_user_registration_register')}}">Registro</a>
            <a class="nav-item nav-link text-muted" href="{{path('fos_user_security_login')}}">Login</a>   
          {% endif %}      

          {% if is_granted("IS_AUTHENTICATED_FULLY") %}
            
            {% if is_granted("ROLE_USER")%}
              <a class="nav-item nav-link text-muted" href="#"> Anfitrion</a> 
              <a href="{{ path('fos_user_profile_show')}}">
              <img src="{{ vich_uploader_asset(app.user, 'imageFile') }}" alt="{{ app.user.username }}" class="rounded-circle border border-secondary" width="40" height="40">
              </a>
              <span> 
                <a class="nav-item nav-link text-muted" href="{{ path('fos_user_profile_show')}}"> {{ app.user.username }}</a>
              </span>
              <span> 
                <a class="nav-item nav-link text-muted" href="{{ path('mascota_index')}}"> Mascota </a>
              </span>                   
            {% endif %}
            
            {% if app.user.username %}
              
            {% endif %}

            {% if is_granted("ROLE_ANFITRION")%}
              <a href="{{ path('fos_user_profile_show')}}">
              <img src="{{ vich_uploader_asset(app.user, 'imageFile') }}" alt="{{ app.user.username }}" class="rounded-circle border border-secondary" width="40" height="40">
              </a>
              <span> 
                <a class="nav-item nav-link text-muted" href="{{ path('fos_user_profile_show')}}">hola anfitrion {{ app.user.username }}</a>
              </span>
              <span> 
                <a class="nav-item nav-link text-muted" href="{{ path('paquete_index')}}"> Paquetes </a>
              </span>                  
            {% endif %}
            
            <span> <a class="nav-item nav-link text-muted" href="{{ path('fos_user_security_logout') }}"> SALIR </a></span>
          
          {% endif %}
        </div>
      </div>
    </div>
    </nav>
  </div>
</div>
<br>
