{% extends 'base.html.twig' %}

{% block title %}New PagoCliente{% endblock %}

{% block body %}
    {{ parent()}}
<div class="container">
    <div class="row justify-content-md-center">
        <div class="col-md-7">
        <h1 class="text-central">Create new PagoCliente</h1>
            {{ include('pago_cliente/_form.html.twig') }}
        </div>
        <div class="col-md-2">
            <a href="{{ path('pago_cliente_index') }}" class="btn btn-info btn-lg">back to list</a>
        </div>
    </div>
</div>
{% endblock %}
