{% extends 'base.html.twig' %}

{% block title %}Edit Paquete{% endblock %}

{% block body %}
    {{ parent()}}

    <h1>Edit Paquete</h1>

    {{ include('paquete/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('paquete_index') }}">back to list</a>

    {{ include('paquete/_delete_form.html.twig') }}
{% endblock %}
