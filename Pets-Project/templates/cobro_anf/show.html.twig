{% extends 'base.html.twig' %}

{% block title %}CobroAnf{% endblock %}

{% block body %}
    <h1>Contenido Cobro Anfitrión</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cobro_anf.id }}</td>
            </tr>
            <tr>
                <th>Comisión</th>
                <td>{{ cobro_anf.comision }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cobro_anf_index') }}">Volver</a>

    <a href="{{ path('cobro_anf_edit', {'id': cobro_anf.id}) }}">Editar</a>

    {{ include('cobro_anf/_delete_form.html.twig') }}
{% endblock %}
